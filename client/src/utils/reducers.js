//! Set up reducers for everything that changes state in the ThreadsDisplay component

function reducer(state, action) {
    switch(action.type) {
        case "pinPost":
            return {

            };
        case "unpinPost":
            return {

            };
        case "createPost":
            return {

            };
        case "deletePost":
            return {

            };
        case "editPost":
            return {

            };
        case "addPostReaction":
            return {

            };
        default:
            return state;
    }
}